<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title>EmpoWered in Tech</title>
    <!-- load local stylesheet (css) -->
    <link rel="stylesheet" href="/styles.css" />
    <!-- loading jquery -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
</head>
<body>
    <%- include("partials/navbar.ejs") %>
    <h1><%= post.name %></h1>
    
    <p><%= post.type %></p>
    <p><%= post.org %></p>
    <p><%= post.subfield %></p>
    <p><%= post.link %></p>

    <p>This post expires on: <% if (post.expiration != null) { %>
        <p><%=post.expiration%></p>
        <% } else { %>
            Never
        <% } %></p>

    <p> Referral Link: <% if (post.referralLink != null) { %>
        <a href="<%= post.referralLink %>"> </a>
        <% } else { %>
            N/A
        <% } %></p>

    <p>Description: <% if (post.description != null) { %>
        <p><%=post.description%></p>
        <% } %></p></p>

    <p>Added by <%=addedBy.name%>, <%= addedBy.email %></p>

    <p>Comments: <% if (post.comments != null) { %>
        <ul><% post.comments.forEach ((elt) => { %>
            <li> <%= elt.author %>: <%= elt.content %></li>
        <% });}; %>
        </ul>

    <%- include("partials/commentForm.ejs") %>

    <% if (addedBy.name == userName) { %>
        <a href="/post/update/<%= post.oid %>" class='button'> Edit this post</a>
    <% } %>
    
    <!-- load local js -->
    <script src="/main.js"></script>
</body>
</html>
